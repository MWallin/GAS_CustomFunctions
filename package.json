{
    "name": "egna_funktioner",
    "version": "1.0.0",
    "description": "",
    "main": "Kod.js",
    "scripts": {
        "publish": "npm run test && npm run build && npm run deploy && npm run version",
        "deploy": "clasp push",
        "build": "gulp build",
        "test": "mocha ",
        "version": "clasp version \"bump\""
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/google-apps-script": "^1.0.14",
        "chai": "^4.2.0",
        "gulp": "^4.0.2",
        "gulp-cli": "^2.3.0",
        "gulp-concat": "^2.6.1",
        "gulp-remove-empty-lines": "^0.1.0",
        "gulp-replace": "^1.0.0",
        "mocha": "^8.0.1"
    }
}
